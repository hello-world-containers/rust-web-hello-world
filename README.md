# Rustlang Hello World Web Container

This is a docker image for Rust's Hello World web image

To run the container from this Docker registry

```
$ docker run -p 8000:8000 helloworldcontainers/rust-web
```